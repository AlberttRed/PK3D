extends Node

export(AudioStream) var COLLISION_SOUND
export(AudioStream) var DOOR_SOUND
export(AudioStream) var CUT_SOUND
export(AudioStream) var SURF_MUSIC
# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.
